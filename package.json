{
  "name": "minimart-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/*"
  ],
  "scripts": {
    "vercel-build": "npm run build --workspace apps/web && node scripts/vercel-sync-next-output.cjs"
  },
  "devDependencies": {
    "next": "^15.5.7"
  }
}
